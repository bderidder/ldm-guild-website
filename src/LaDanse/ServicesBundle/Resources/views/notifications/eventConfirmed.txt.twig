Confirmed!

The organiser of the event '{{ data.activityData.event.name }}' you signed up for, confirmed the event!

When: {{ data.activityData.event.inviteTime|date("d/m (D)") }}

Invite Time: {{ data.activityData.event.inviteTime|date("H:i") }}
Start Time: {{ data.activityData.event.startTime|date("H:i") }}

End Time: {{ data.activityData.event.endTime|date("H:i") }}

{{ data.activityData.event.description }}

Make sure to properly prepare yourself. Read up on tactics and bring flasks, pots and runes with you!

{{ url('viewEvent', {'id': data.activityData.event.eventId} ) }}



You received this email because of your current notifications settings. You can view or change your
notification settings at the following URL:

{{ url('editNotifications') }}