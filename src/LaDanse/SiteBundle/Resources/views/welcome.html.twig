{% extends 'LaDanseSiteBundle:Layout:layout.html.twig' %}

{% block title %}La Danse - Index{% endblock %}

{% block body %}

    <h2>Recently created or modified events</h2>

    <p>Events that have been added in the last N days and haven't passed yet.</p>

    <h2>My sign ups</h2>

    <p>A list of signups for the next N days.</p>

    <h2>Other actions on this page</h2>
    
    <ul>
        <li><a href="{{ path('viewEventsIndex') }}">Events</a></li>
        {% if auth.currentContext.authenticated %}
            <li><a href="{{ path('mySettingsIndex') }}">My settings</a></li>
            <li><a href="{{ path('viewSignUpsIndex') }}">My sign ups</a></li>       
        {% endif %}
    </ul>

    {% if app.environment == 'dev' %}
        <p>Development only:</p>
        <ul>
            <li><a href="{{ path('loginuser', {'id': 1}) }}">Log in as 1</a></li>
            <li><a href="{{ path('loginuser', {'id': 2}) }}">Log in as 2</a></li>
            <li><a href="{{ path('logoutuser') }}">Log out</a></li>
        </ul>
    {% endif %} 

{% endblock %}
