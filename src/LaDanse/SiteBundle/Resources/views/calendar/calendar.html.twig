{% extends 'LaDanseSiteBundle:layout:layout.html.twig' %}

{% block title %}La Danse - Calendar{% endblock %}

{% block body %}

	<ol class="breadcrumb">
        <li><a href="{{ path('welcomeIndex') }}">Home</a></li>
        <li class="active">Calendar</li>
    </ol>

    <h1>Raids</h1>

    <!--a href="{{ path('createEventIndex') }}" class="btn btn-primary active" role="button">Create Raid</a -->

    {{ render(controller('LaDanseSiteBundle:Calendar/Calendar:indexPartial')) }}

{% endblock %}
